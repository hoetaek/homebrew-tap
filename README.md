# homebrew-tap

Homebrew formulae maintained by `hoetaek`.

## Formulae

- `wt`: Git worktree workspace manager
- `doist`: unofficial command line app for interacting with the Todoist API

## Install

```bash
brew install hoetaek/tap/wt
```
